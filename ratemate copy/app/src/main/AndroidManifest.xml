<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ratemate">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Application Declaration -->
    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.RateMate"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"> <!-- For testing purposes; set to false for production -->

        <!-- OpenStreetMap Configuration -->
        <meta-data
            android:name="org.osmdroid.config"
            android:value="MAPNIK" />

        <!-- Splash Activity (Video Splash Screen) -->
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (Login Screen) -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true"
            tools:ignore="RedundantLabel" />

        <!-- Home Activity (After successful login) -->
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true"
            tools:ignore="Instantiatable,RedundantLabel" />

        <!-- Signup Activity (For user registration) -->
        <activity
            android:name=".SignupActivity"
            android:label="@string/SignUp"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true"
            tools:ignore="Instantiatable" />

        <!-- Profile Activity (For user profile and password reset) -->
        <activity
            android:name=".ProfileActivity"
            android:label="Profile"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true"
            tools:ignore="Instantiatable" />

    </application>
</manifest>